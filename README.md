# Короче инструкция по установке 

Ну блин, тут телеграм бот для работы с базой контактов. Короче делаешь так:

## Установка (не сложно вроде)

1. `python -m venv venv` - создаешь виртуалку
2. `source venv/bin/activate` - активируешь (на линуксе/маке) 
   или `venv\Scripts\activate` если винда
3. `pip install -r requirements.txt` - ставишь либы
4. Копируешь `.env.example` в `.env`, меняешь токен на свой и ставишь `ADMIN_ID` — свой Telegram ID
5. `python bot.py` - запускаешь

## Что делает

Берет контакты из `base.csv` и показывает по одному. Ты жмешь:
- ✅ если все ок - кидает в `verified.csv` 
- ❌ если хрень какая-то - в `called.csv`

Из основной базы контакт удаляется чтоб не повторялся.

## Команды бота

- `/start` - начинаешь работу
- `/next` - следующий контакт если что
- `/reload` - перезагрузить базу

## Примечания

Не забудь в `.env` поменять `BOT_TOKEN` и `ADMIN_ID` на свои данные. А то работать не будет. И база должна быть в формате "Имя Фамилия,+7999000000" в каждой строке.

Работает только для того аккаунта, чей `ADMIN_ID` указан. Остальным бот пишет "Нет доступа".

Короче все, удачи 🤞